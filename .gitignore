# Local tooling and OS metadata
.DS_Store
.codegraph/
.codex/
.claude/
.qoder/
docs/superpowers/plans/
openspec/
stage/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# rocprof-trace-decoder is cloned by install.sh at runtime
tools/rocprof-trace-decoder/

# Git submodules are tracked via .gitmodules
# reference-projects/ content is managed as submodules

# Run roots created by orchestrator/optimize.py live at --prefix (default /tmp/aka-opt, outside the
# repo). If you point --prefix at a directory inside the repo, add that directory here yourself.
