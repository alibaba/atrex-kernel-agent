# Teacher bundle example

`minimal_teacher/` demonstrates the required bundle structure and provenance fields. It is a structural example, not a benchmark target for an arbitrary operator. A real Teacher must implement the exact evaluator contract of the selected `--op-dir`, use the same framework and architecture as the Candidate campaign, and pass full correctness before optimization starts.
