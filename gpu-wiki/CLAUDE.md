# GPU Wiki Compatibility Index

Read `README.md` first. Use `python3 gpu-wiki/scripts/query.py` with explicit architecture/vendor/DSL filters before broad grep. This wiki is product-neutral: use available read/search tools, prefer vendor official documentation and `reference-projects/` for API and ISA ground truth, and run the self-containment and query tests after path or index changes.
