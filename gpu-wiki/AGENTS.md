# GPU Wiki Agent Entry

Read `README.md` first. Follow the self-containment contract there: use relative in-wiki links, treat external checkouts as explicit environment variables, and run `python3 gpu-wiki/scripts/check-self-contained.py --root gpu-wiki` after path or index changes.
